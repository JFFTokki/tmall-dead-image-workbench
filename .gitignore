# Byte-compiled / cache
__pycache__/
*.pyc
.npm-cache/

# Vendored third-party packages (reinstall via pip instead of tracking binaries)
vendor/

# Credentials - never commit session/login data
*.har

# Large source data files - too big for version history, keep locally only
*.xlsx

# Run outputs and test outputs (regenerated by the workbench, not source)
dead_image_output/runs/
dead_image_output/workbench_runs/
死图测试/

# Local AI tool state, unrelated to project source
.agents/
.codex/
.superpowers/

# Business data - real product IDs / store screenshots / marketing templates
dead_image_output/models/current/dead_image_model_feedback.json
死图.png
template_reference/

# OS
Thumbs.db
